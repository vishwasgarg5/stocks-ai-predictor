name: NIFTY 150 Morning Prediction

on:
  workflow_dispatch:
  schedule:
    # 02:15 UTC = 07:45 IST: 90 minutes before NSE opens at 09:15 IST.
    - cron: "15 2 * * 1-5"

permissions:
  contents: write

concurrency:
  group: nifty150-daily-pipeline
  cancel-in-progress: false

env:
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  predict:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Predict today's session using prior-session data only
        run: python morning.py
      - name: Commit prediction ledger and Nifty 150 cache
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data reports
          if git diff --cached --quiet; then echo "No morning prediction changes."; exit 0; fi
          git commit -m "chore: save morning NIFTY 150 prediction"
          git push
